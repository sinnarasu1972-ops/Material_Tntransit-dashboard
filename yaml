services:
  - type: web
    name: unnati-material-transit
    env: python
    plan: free
    pythonVersion: 3.12.10
    
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port 8000
    
    healthCheckPath: /
    
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.10
